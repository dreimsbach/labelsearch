services:
  labelsearch:
    build: .
    image: label-release-tracker:latest
    ports:
      - "8787:8787"
    environment:
      PORT: "8787"
      NODE_ENV: "production"
      MB_USER_AGENT: "label-release-tracker/0.1.0 (local@docker)"
      LOG_FILE_PATH: "/app/logs/app.log"
